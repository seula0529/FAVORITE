import { BEANS } from '@/data/beans.js'

// ─────────────────────────────────────────────
//  진단 결과 매핑 — 이번 단계 구현 범위 밖 (자리만 확보)
//
//  TODO: 응답 → 원두 유형 판정 로직 구현
//    - answers: [{ id: 'q1', value: 'a' }, ...] 형태로 들어온다
//      (value 는 questions.js 의 보기 순서 고정 키 a~d)
//    - 가중치 합산 / 조합 매핑 등 확정되는 방식에 맞춰 이 함수 내부만 채우면 된다.
//    - 반환값은 BEANS 배열의 한 항목(원두 객체)이어야 한다.
//
//  현재는 검증용으로 항상 첫 번째 유형을 반환한다.
// ─────────────────────────────────────────────

export function getResultType(answers = []) {
  void answers // 로직 구현 시 사용
  return BEANS[0]
}
